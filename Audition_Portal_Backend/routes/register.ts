import express from "express";
import { Router } from "express";
// import { handleNewMember } from "../controllers/registerController";

const router: Router = express.Router();

// router.use("/", handleNewMember);

export default router;
